{% extends 'base.html.twig' %}

{% block title  %}
    <h1>{{ spot.name }}</h1>
{% endblock %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>{{ spot.name }}</h1>
    </div>

    <div class="container">
        {{ spot.description|raw }}
    </div>
{% endblock %}